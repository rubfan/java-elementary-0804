package liskovsSubstitutionPrinciple;

import defaultState.User;

public class IsValidUserYesLSP extends UserRegistrationArrayListLSP {
    @Override
    public boolean isValid(User user) {
        return super.isValid(user);
    }
}
