package AnimalInfo_Test;
import AnimalInfo.AnimalInfo;
import org.junit.*;

import java.util.Scanner;

public class Programm12_2_Test {
    static String name;

    @Test
   "Spiders".equals(name)) {
    AnimalInfo.Spiders spiders = new AnimalInfo.AnimalInfo.Spiders();
        spiders.getInfo(name);

        @Test
    } else if ("Bears".equals(name)) {
        AnimalInfo.AnimalInfo.Bears bears = new AnimalInfo.AnimalInfo.Bears();
        bears.getInfo(name);
    }
}
        };

}
