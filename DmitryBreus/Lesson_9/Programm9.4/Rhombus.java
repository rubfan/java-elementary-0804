public class Rhombus {
    public double getArea(int d1, int d2) {
        return (d1 + d2) / 2;
    }
    public double getPerimeter(int d1, int d2) {
        return 2 * Math.sqrt((Math.pow(d1, 2) + Math.pow(d1, 2)));
    }
}
