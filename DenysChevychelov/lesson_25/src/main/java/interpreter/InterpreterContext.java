package interpreter;

public class InterpreterContext {
    public String convertToUpperCase(String exp) {
        return exp.toUpperCase();
    }

    public String convertToLowerCase(String exp) {
        return exp.toLowerCase();
    }
}
